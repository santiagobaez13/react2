import cart from './assets/cart.svg'
const CartWidget = () =>{

    return (
        <div>
           <img src={cart} alt="cart-widget" style={{ height: '20px' }} />

        </div>
    )
}

export default CartWidget